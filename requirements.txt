fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
pydantic>=2.6
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
pillow>=10.2
imagehash>=4.3
anthropic>=0.34
google-genai>=0.7
pandas>=2.2
python-multipart>=0.0.9

# Optional: only needed if you set SCRAPER_RENDER_JS=true to render
# JavaScript-heavy pages in the fallback scraper. After installing, run
# `playwright install chromium` once.
# playwright>=1.42
